[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.black]
exclude = 'venv'

[tool.isort]
profile = "black"
multi_line_output = 3
skip = ["venv"]
